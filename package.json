{
  "name": "holiday_hub",
  "version": "1.0.0",
  "description": "Social media app to plan group holidays and share memories",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix server",
    "install-client": "npm install --prefix client",
    "server": "npm run server --prefix server",
    "client": "cd client && ng serve",
    "deploy": "npm run build --prefix client && ng serve --project server",
    "test": "npm test --prefix server && ng test --project client"
  },
  "keywords": [],
  "author": "Adam Duggan",
  "license": "ISC"
}

